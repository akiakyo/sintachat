"use client";
import {FormEvent,useState} from "react";import SiteHeader from "../../components/SiteHeader";import SiteFooter from "../../components/SiteFooter";import {getSessionId} from "../../lib/session";const API=()=>process.env.NEXT_PUBLIC_SOCKET_URL||"http://localhost:3001";export default function Appeal(){const[msg,setMsg]=useState("");const[status,setStatus]=useState("");async function submit(e:FormEvent){e.preventDefault();const r=await fetch(`${API()}/api/appeals`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sessionId:getSessionId(),message:msg})});const d=await r.json();setStatus(r.ok?`Appeal submitted. Case ID: ${d.appealId}`:d.error||"Could not submit appeal.");if(r.ok)setMsg("")}return <><SiteHeader/><main className="appeal-page"><section className="appeal-card"><p className="eyebrow">MODERATION APPEAL</p><h1>Appeal a restriction</h1><p>Keep your appeal anonymous. Do not include your real name, address, phone number, or other personal information.</p><form onSubmit={submit}><textarea value={msg} onChange={e=>setMsg(e.target.value)} maxLength={1000} placeholder="Explain why the ban or suspension should be reviewed."/><div><span>{msg.length}/1000</span><button>Submit appeal</button></div></form>{status&&<div className="appeal-status">{status}</div>}</section></main><SiteFooter/></>}
